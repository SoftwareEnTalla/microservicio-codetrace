services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - entalla-net

  # Producción: habilitar Postgres privado por microservicio
  # postgres:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_DB: ${DB_NAME}
  #     POSTGRES_USER: ${DB_USERNAME}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   networks:
  #     - entalla-net

networks:
  entalla-net:
    external: true

volumes:
  pgdata:
