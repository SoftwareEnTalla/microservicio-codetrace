services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - entalla-net
      - shared-net

networks:
  entalla-net:
    driver: bridge
  shared-net:
    external: true
